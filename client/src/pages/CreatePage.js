import React from 'react';
import CreateForm from "../components/Create/CreateForm";

const CreatePage = () => {
    return (
        <div>
            <CreateForm/>
        </div>
    );
};

export default CreatePage;